<% stuff = typeof stuff !== 'object' ? {} : stuff %>
<!-- Left side column. contains the logo and sidebar -->
<aside class="main-sidebar">

  <!-- sidebar: style can be found in sidebar.less -->
  <section class="sidebar">

    <!-- Sidebar user panel (optional) -->
    <div class="user-panel">
      <div class="pull-left image">
        <img src="/adminlte/dist/img/user2-160x160.jpg" class="img-circle" alt="User Image">
      </div>
      <div class="pull-left info">
        <p>Alexander Pierce</p>
        <!-- Status -->
        <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
      </div>
    </div>

    <!-- search form (Optional) -->
    <form action="#" method="get" class="sidebar-form">
      <div class="input-group">
        <input type="text" name="q" class="form-control" placeholder="Search...">
        <span class="input-group-btn">
          <button type="submit" name="search" id="search-btn" class="btn btn-flat"><i class="fa fa-search"></i>
          </button>
        </span>
      </div>
    </form>
    <!-- /.search form -->

    <!-- Sidebar Menu -->
    <ul class="sidebar-menu" data-widget="tree">
      <!-- Optionally, you can add icons to the links -->
      <li class="<% if ( stuff.url == "/admin" ) { %> active <% } %>">
        <a href="/admin"> <i class="fa fa-dashboard"></i> <span>Dashboard</span></a>
      </li>
      <li class="<% if ( stuff.url == "/admin/mobil" ) { %> active <% } %>">
        <a href="/admin/mobil"><i class="fa fa-user"></i> <span>Mobil</span></a>
      </li>
      <li class="<% if ( stuff.url == "/admin/jadwal" ) { %> active <% } %>">
        <a href="/admin/jadwal"><i class="fa fa-users"></i> <span>Jadwal</span></a>
      </li>
      <li class="<% if ( stuff.url == "/admin/mata-pelajaran" ) { %> active <% } %>">
        <a href="/admin/mata-pelajaran"><i class="fa fa-link"></i> <span>Mata Pelajaran</span></a>
      </li>
      <li class="<% if ( stuff.url == "/admin/kelas" ) { %> active <% } %>">
        <a href="/admin/kelas"><i class="fa fa-home"></i> <span>Kelas</span></a>
      </li>

    </ul>
    <!-- /.sidebar-menu -->
  </section>
  <!-- /.sidebar -->
</aside>